name: Test Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: install dependencies (ubuntu)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev

      - name: check formatting
        working-directory: ./src-tauri
        run: cargo fmt --check

      - name: clippy
        working-directory: ./src-tauri
        run: cargo clippy -- -D warnings

      - name: run tests
        working-directory: ./src-tauri
        run: cargo test --lib

  build:
    needs: [check]
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - platform: windows-latest
            target: x86_64-pc-windows-msvc

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: get version
        id: get-version
        shell: bash
        run: echo "version=$(node -pe "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libfuse2 file

      - name: install frontend dependencies
        run: npm ci
        working-directory: .

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: "."
          includeUpdaterJson: false

      - name: Package Windows Portable
        if: matrix.platform == 'windows-latest'
        shell: bash
        run: |
          VERSION="${{ steps.get-version.outputs.version }}"
          mkdir -p portable-staging
          cp "src-tauri/target/release/app.exe" "portable-staging/NWN2EE Save Editor.exe"
          7z a "src-tauri/target/release/bundle/nsis/NWN2EE-Save-Editor-${VERSION}-windows-Portable.zip" ./portable-staging/*

      - name: Upload AppImage
        if: matrix.platform == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: NWN2EE-Editor-${{ steps.get-version.outputs.version }}-AppImage
          path: src-tauri/target/release/bundle/appimage/*.AppImage
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Deb
        if: matrix.platform == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: NWN2EE-Editor-${{ steps.get-version.outputs.version }}-Deb
          path: src-tauri/target/release/bundle/deb/*.deb
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Windows Installer
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: NWN2EE-Editor-${{ steps.get-version.outputs.version }}-Windows-Installer
          path: src-tauri/target/release/bundle/nsis/*.exe
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Windows Portable
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: NWN2EE-Editor-${{ steps.get-version.outputs.version }}-windows-Portable
          path: src-tauri/target/release/bundle/nsis/*.zip
          retention-days: 7
          if-no-files-found: ignore
